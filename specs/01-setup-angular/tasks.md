# Tasks: Angular Project Setup

**Skill**: `skills/01-setup-angular.md`

---

## Phase 1 — Project Init

- T001 Create Angular 17+ project with standalone components, strict TypeScript, no routing (added separately)
- T002 [P] Set up folder structure: `core/`, `shared/`, `layout/`, `features/`
- T003 [P] Add Inter font via Google Fonts link in `index.html`
- T004 Create `src/styles.css` with all CSS custom properties (colors, spacing, layout, typography, shadows, radii)

**Checkpoint**: `ng serve` runs. Global tokens are importable. Folder structure matches `structure.md`.
